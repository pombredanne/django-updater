============
Installation
============

At the command line::

    $ easy_install django-updater

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-updater
    $ pip install django-updater
